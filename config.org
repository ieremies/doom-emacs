#+Title: My Config
#+Property: header-args :tangle config.el
#+Property: header-args :results silent
#+Startup: show2levels

* General
** Compile-angel
Ensure all loaded .el files are compiled beforehand.
#+begin_src elisp :tangle packages.el
(package! compile-angel)
#+end_src

This should be in ~early-init.el~, but I am not sure how to do so in doom's emacs:
#+begin_src elisp
(setq load-prefer-newer t)
#+end_src

#+begin_src elisp
(use-package compile-angel
  :demand t
  :config
  ;; Uncomment the line below to compile automatically when an Elisp file is saved
  (add-hook 'emacs-lisp-mode-hook #'compile-angel-on-save-local-mode)

  ;; The following directive prevents compile-angel from compiling your init
  ;; files. If you choose to remove this push to `compile-angel-excluded-files'
  ;; and compile your pre/post-init files, ensure you understand the
  ;; implications and thoroughly test your code. For example, if you're using
  ;; the `use-package' macro, you'll need to explicitly add:
  ;; (eval-when-compile (require 'use-package))
  ;; at the top of your init file.
  (push "/init.el"       compile-angel-excluded-files)
  (push "/early-init.el" compile-angel-excluded-files)

  ;; A global mode that compiles .el files before they are loaded
  ;; using `load' or `require'.
  (compile-angel-on-load-mode 1))
#+end_src
** General
#+begin_src elisp
;; --- Global Settings ---
(setq user-full-name "Ieremies V. F. Romero"
      user-mail-address "ieremies@gmail.com"
      doom-font (font-spec :family "JetBrainsMono Nerd Font" :size 14)
      doom-theme 'nano-light
      delete-by-moving-to-trash t
      display-line-numbers-type nil

      ;; NOTE this shouldn't be here
      evil-want-fine-undo 't
      truncate-string-ellipsis "…"
      widget-image-enable 'nil
      x-underline-at-descent-line 't)

;; --- UI/Frame Settings ---
(menu-bar-mode -1)
(window-divider-mode 1)

(set-face-attribute 'variable-pitch nil
                    :family "Hanken Grotesk"
                    ;; Other options:
                    ;; :family "NotoSans Nerd Font"
                    ;; :family "SF Pro Text"
                    ;; :family "Helvetica"
                    :height 150)

(setq-default delete-by-moving-to-trash t
              window-combination-resize t
              underline-minimum-offset 3
              x-underline-at-descent-line nil
              line-spacing 2
              menu-bar-mode -1)

(window-divider-mode 1)
(menu-bar-mode -1)
#+end_src

Some packages invoke the shell and the load of the respective rc file can be considerable.
#+begin_src elisp
(setq inhibit-compacting-font-caches t
      display-line-numbers-type nil
      explicit-shell-file-name "/bin/bash")
#+end_src

Configuration for MacOS.
#+begin_src elisp
(setq frame-title-format " "
      ns-use-proxy-icon 'nil
      ns-use-native-fullscreen 'nil)
#+end_src

Default window configuration.
#+begin_src elisp
(setq window-min-height 1
      window-divider-default-right-width 24
      window-divider-default-places 'right-only
      default-frame-alist (append (list
                                   '(height     . 80)
                                   '(width      . 98)
                                   '(vertical-scroll-bars  . nil)
                                   '(internal-border-width . 24)
                                   '(undecorated-round     . t))))
#+end_src

** Nano-modeline
#+begin_src elisp :tangle packages.el
(package! doom-nano-modeline
  :recipe (:host github :repo "ronisbr/doom-nano-modeline"))
#+end_src
#+begin_src elisp
(use-package! doom-nano-modeline
  :config
  (doom-nano-modeline-mode 1))
#+end_src

#+begin_src emacs-lisp :tangle packages.el
(package! hide-mode-line)
#+end_src
#+begin_src emacs-lisp :tangle yes
(use-package! hide-mode-line
  :config
  ;; This runs the function whenever a new buffer is created
  (add-hook 'after-change-major-mode-hook #'hide-mode-line-mode))
#+end_src

* Checkers
** Spell
*** Jinx
#+begin_src elisp :tangle packages.el
(package! jinx
  :recipe (:host github :repo "minad/jinx"))
#+end_src
#+begin_src elisp
(use-package! jinx
  ;; :hook
  ;; (org-mode      . jinx-mode)
  ;; (typst-ts-mode . jinx-mode)
  :config
  (setq jinx-languages "en_US"
        jinx-delay 1.0)
  (nconc (cadr jinx-exclude-faces) '(org-ref-ref-face)))

(after! vertico-multiform
  (add-to-list 'vertico-multiform-categories
               '(jinx (vertico-grid-annotate . 25)))
  (vertico-multiform-mode 1))

(with-eval-after-load 'jinx
  (define-key jinx-overlay-map
    [down-mouse-1]
    `(menu-item "" (keymap) :filter ,#'jinx--correct-menu)))
#+end_src

* Completion
** corfu
#+begin_src emacs-lisp :tangle yes
(after! corfu
  (setq corfu-auto-delay 0.2
        text-mode-ispell-word-completion nil
        corfu-popupinfo-delay '(0.5 . 0.2)
        corfu-count 10))
#+end_src

** hotfuzz + orderless
#+begin_src elisp :tangle packages.el
(package! hotfuzz
  :recipe (:host github :repo "axelf4/hotfuzz"))
#+end_src
#+begin_src elisp
;;;###autoload
(defun hotfuzz+orderless-all-completions (str table pred pt)
  (let ((words (split-string str nil t)))
    (orderless-all-completions
     (string-join (cdr words) " ")
     (if (car words)
         (nconc (hotfuzz-all-completions (car words) table pred pt) nil)
       table)
     pred pt)))

;;;###autoload
(progn
  (add-to-list 'completion-styles-alist
               '( hotfuzz+orderless orderless-try-completion hotfuzz+orderless-all-completions
                  "Filter first component with Hotfuzz, rest with Orderless."))
  (put 'hotfuzz+orderless 'completion--adjust-metadata #'hotfuzz--adjust-metadata))
#+end_src
* Editor
** Evil
#+begin_src emacs-lisp :tangle yes
(after! evil
  (setq evil-ex-substitute-global t
        evil-kill-on-visual-paste nil
        evil-vsplit-window-right  t
        evil-split-window-below   t)
  ;; Center cursor on screen after search
  (advice-add 'evil-ex-search-next :after
              (lambda (&rest x) (evil-scroll-line-to-center (line-number-at-pos))))
  (advice-add 'evil-ex-search-previous :after
              (lambda (&rest x) (evil-scroll-line-to-center (line-number-at-pos)))))
#+end_src
* Emacs
** buffer-terminator
#+begin_src emacs-lisp :tangle packages.el
(package! buffer-terminator)
#+end_src
#+begin_src emacs-lisp :tangle yes
(use-package buffer-terminator
  :custom
  (buffer-terminator-verbose nil)

  ;; Set the inactivity timeout (in seconds) after which buffers are considered
  ;; inactive (default is 30 minutes):
  (buffer-terminator-inactivity-timeout (* 30 60)) ; 30 minutes

  ;; Define how frequently the cleanup process should run (default is every 10
  ;; minutes):
  (buffer-terminator-interval (* 10 60)) ; 10 minutes

  :config
  (buffer-terminator-mode 1))
#+end_src
** super-save
#+begin_src emacs-lisp :tangle packages.el
(package! super-save)
#+end_src
#+begin_src emacs-lisp :tangle yes
(use-package super-save
  :ensure t
  :config
  (super-save-mode +1)
  (auto-save-visited-mode +1) ;; for idle saves
  (setq auto-save-default nil
        super-save-silent t
        super-save-all-buffer t))
#+end_src
** smart-parens
#+begin_src emacs-lisp :tangle yes
(after! smartparens
  (sp-local-pair 'org-mode "$" "$"))
#+end_src

* Email
#+begin_src emacs-lisp :tangle yes
(after! mu4e
  (setq mu4e-get-mail-command "mbsync -a"
        mu4e-update-interval       300
        mu4e-compose-format-flowed t
        mu4e-index-cleanup         nil
        mu4e-index-lazy-check      t
        ;; TODO change to assync
        send-mail-function         'smtpmail-send-it
        message-send-mail-function 'smtpmail-send-it
        ;; TODO mail queue for offline send
        ;; smtpmail-queue-mail        nil
        ;; smtpmail-queue-dir         "/home/user/Maildir/queue/cur"
        )

  (setq mu4e-contexts
        (list
         ;; --- GMAIL CONTEXT ---
         (make-mu4e-context
          :name "gmail"
          :match-func (lambda (msg)
                        (when msg
                          (or (mu4e-message-contact-field-matches msg :to "ieremies@gmail.com")
                              (mu4e-message-contact-field-matches msg :cc "ieremies@gmail.com"))))
          :vars '((user-mail-address      . "ieremies@gmail.com")
                  (user-full-name         . "Ieremies")
                  (smtpmail-smtp-user     . "ieremies@gmail.com")
                  (smtpmail-smtp-server   . "smtp.gmail.com")
                  ;; Folders
                  (mu4e-sent-folder       . "/[Gmail]/Sent Mail")
                  (mu4e-drafts-folder     . "/[Gmail]/Drafts")
                  (mu4e-trash-folder      . "/[Gmail]/Trash")
                  (mu4e-refile-folder     . "/[Gmail]/All Mail")))

         ;; --- UNICAMP CONTEXT ---
         (make-mu4e-context
          :name "unicamp"
          :match-func (lambda (msg)
                        (when msg
                          (or (mu4e-message-contact-field-matches msg :to "ieremies@ic.unicamp.br")
                              (mu4e-message-contact-field-matches msg :cc "ieremies@ic.unicamp.br"))))
          :vars '((user-mail-address      . "ieremies@ic.unicamp.br")
                  (user-full-name         . "Ieremies")
                  (smtpmail-smtp-user     . "ieremies@gmail.com")
                  (smtpmail-smtp-server   . "smtp.gmail.com")
                  ;; Folders (Same as Gmail since they share the Maildir)
                  (mu4e-sent-folder       . "/[Gmail]/Sent Mail")
                  (mu4e-drafts-folder     . "/[Gmail]/Drafts")
                  (mu4e-trash-folder      . "/[Gmail]/Trash")
                  (mu4e-refile-folder     . "/[Gmail]/All Mail")))))

  ;; 3. CONTEXT POLICIES
  (setq mu4e-context-policy 'pick-first
        mu4e-compose-context-policy 'ask))
#+end_src
- Email offline queue
- Message signature?
- Quando enviando emails, ele utiliza ~org-msg~ para transformar em HTML a mensagem.  
  - Isso deixa algumas coisas estranhas, mas é necessário para enviar coisas como formatação, código etc... 
  - Preciso decidir o que fazer com isso.

When configuring the email, this error appeared:
#+begin_src emacs-lisp :tangle yes
(after! doom-themes
  (setcdr (assoc 'gnus-group-news-low-empty doom-themes-base-faces)
          '(:inherit 'gnus-group-mail-1-empty :weight 'normal)))
#+end_src
* Languages
** CC
#+begin_src emacs-lisp :tangle yes
(setq my/clangd-args
      '("-j=3"
        "--background-index"
        "--clang-tidy"
        "--completion-style=detailed"
        "--header-insertion=never"
        "--header-insertion-decorators=0"))

(after! (lsp-mode lsp-clangd)
  (setq lsp-clients-clangd-args my/clangd-args)
  (set-lsp-priority! 'clangd 2))

(after! eglot
  (set-eglot-client! 'cc-mode (append '("clangd") my/clangd-args)))
#+end_src
** LaTeX
#+begin_src emacs-lisp :tangle yes
(after! tex
  (setq TeX-engine-alist '((default
                            "Tectonic"
                            "tectonic -X compile -f plain %T"
                            "tectonic -X watch"
                            nil))
        LaTeX-command-style '(("" "%(latex)"))
        TeX-process-asynchronous t
        TeX-check-TeX nil
        TeX-engine 'default)

  (let ((tex-list (assoc "TeX" TeX-command-list))
        (latex-list (assoc "LaTeX" TeX-command-list)))
    (setf (cadr tex-list) "%(tex)"
          (cadr latex-list) "%l")))
#+end_src
** Python
#+begin_src emacs-lisp :tangle no
(after! apheleia
  (setf (alist-get 'python-mode apheleia-mode-alist)
        '(ruff-isort ruff))
  (setf (alist-get 'python-ts-mode apheleia-mode-alist)
        '(ruff-isort ruff)))
#+end_src
* org-mode
** latex
#+begin_src elisp
(after! org
  (setq org-highlight-latex-and-related '(native script entities)))
#+end_src
* UI
** Mixed pitch
The =mixed-pitch-mode= reverts back to the default =fixed-pitch :weight= value when a face is in =mixed-pitch-fixed-pitch-faces=.
#+begin_src diff :tangle no
-    face :family fix-pitch :height fix-height :weight fix-weight)
- (face-remap-add-relative face :family fix-pitch :weight fix-weight))))
+    face :family fix-pitch :height fix-height)
+ (face-remap-add-relative face :family fix-pitch))))
#+end_src
#+begin_src emacs-lisp :tangle packages.el
(package! mixed-pitch)
#+end_src
#+begin_src emacs-lisp :tangle no
(use-package! mixed-pitch
  :hook
  (org-mode . mixed-pitch-mode)
  (typst-ts-mode . mixed-pitch-mode)
  :config
  (setq mixed-pitch-set-height 't))
#+end_src
** Olivetti
#+begin_src emacs-lisp :tangle packages.el
(package! olivetti)
(package! auto-olivetti
  :recipe (:host sourcehut :repo "ashton314/auto-olivetti"))
#+end_src
#+begin_src emacs-lisp :tangle yes
(use-package! olivetti
  :hook (text-mode . olivetti-mode)
  :config (setq-default olivetti-body-width 99))
#+end_src
* Tools
** [[https://github.com/SavchenkoValeriy/emacs-powerthesaurus][SavchenkoValeriy/emacs-powerthesaurus]]
Use =powerthesaurus-lookup-dwim=.
#+begin_src elisp :tangle packages.el
(package! powerthesaurus)
#+end_src
** Debugger
DOOM's also has a debugger module, but dap-mode is the standard.
*** [[https://github.com/svaante/dape][svaante/dape]]
I have to use [[https://github.com/vadimcn/codelldb/blob/v1.10.0/MANUAL.md][codelldb]].
#+begin_src elisp :tangle packages.el
(package! dape-mode
  :recipe (:host github :repo "svaante/dape"))
#+end_src
#+begin_src elisp
(use-package dape
  :hook
  ((kill-emacs . dape-breakpoint-save)
   (after-init . dape-breakpoint-load))
  :config
  (setq dape-buffer-window-arrangement 'right
        dape-cwd-fn 'projectile-project-root)
  (dape-breakpoint-global-mode))
#+end_src
** LSP (eglot or LSP-mode)
#+begin_src emacs-lisp :tangle yes
(after! lsp-mode
  (add-hook! 'lsp-mode-hook (lambda () (lsp-inlay-hints-mode 1))))
#+end_src
[[https://www.reddit.com/r/emacs/comments/1n3o2we/on_macos_in_gui_with_eglot_turned_on_when_the/][See this]]
#+begin_src emacs-lisp :tangle yes
(after! eglot
  ;; 
  (setq eglot-code-action-indicator "*"))
#+end_src
* AI
** Completion / copilot
It causes around 0.3s of startup time.
#+begin_src elisp :tangle packages.el
(package! copilot
  :recipe (:host github :repo "copilot-emacs/copilot.el" :files ("*.el")))
#+end_src
#+begin_src elisp
(use-package! copilot
  :hook (prog-mode . copilot-mode)
  :bind (:map copilot-completion-map
              ("C-[" . 'copilot-complete)
              ("C-]" . 'copilot-accept-completion)
              ("C-}" . 'copilot-accept-completion-by-word))
  :config
  (setq copilot-idle-delay 0.2))
#+end_src
=======
